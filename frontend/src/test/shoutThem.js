export default (givenArray) => givenArray.map(item => item.toUpperCase() + '!');


    /*let shoutOutLoud = [];
    for (let i = 0; i < givenArray.length; i++) {
        shoutOutLoud[i] = givenArray[i].toUpperCase() + '!';
        //console.log(changeItem);
        //shoutOutLoud.push(changeItem);
    }
    return shoutOutLoud;

     */

